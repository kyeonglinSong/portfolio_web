<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>certification detail</title> 
        <script type="text/javascript" src="app.js"></script>
    </head> 
    <body style="background-color: lightgray;">
        <div style="text-align: center;">
            <h1>certifications</h1>
            <hr>
        </div>
        <div>
            <h2>자격증 상세</h2>
            <p> 
                <h4>자격증 이름</h4>
                <%= JSON.stringify(post.cert_name) %>
                <h4>자격증 번호</h4>
                <%= JSON.stringify(post.cert_num) %>
                <h4>발급기관</h4>
                <%= JSON.stringify(post.issuing_org) %>
                <h4>발급일자</h4>
                <%= JSON.stringify(post.issuing_date) %>
                <% if(post.expiration_date) { %>
                <h4>만료일자</h4>
                <%= JSON.stringify(post.expiration_date) %>
               <% } %>
            </p>
        </div>
        <div style="text-align: center;">
            <a href='/certification/ <%= post.seq %>/delete'> <input type="button" value="글 삭제"> </a>
            <a href='/certification/<%= post.seq %>/edit'> <input type="button" value="글 수정"> </a>
            <a href='../main'> <input type="button" value="main"> </a>
        </div>
    </body>
</html>