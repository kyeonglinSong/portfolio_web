<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>project</title> 
        <script type="text/javascript" src="app.js"></script>
    </head>
    <body>
        <div style="text-align: center;">
            <h1> Project List</h1>
            <hr>
        </div>
        <div>
            <h2>Name of Project</h2>
            Press the project name to go to the detailed view.
            <br><br><br>
            <% for (let i=0 ; i < posts.length ; i++) { %>
            <p> 
                <a href='/project/<%= posts[i].seq %>'><%= JSON.stringify(posts[i].proj_name) %></a> 
                <br>
                <% if(posts[i].url) { %>
                Related URL : <%= JSON.stringify(posts[i].url) %></p>
                <% } %>
            <br>
            <%}%>
        </div>
        <div style="text-align: center;">
            <p>
                <a href="/project/add"><input type="button" value="add project"></a>
                <a href='../main'> <input type="button" value="main"> </a>
            </p>
        </div>
    </body>
</html>