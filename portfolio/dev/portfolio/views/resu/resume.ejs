<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>resume</title> 
        <script type="text/javascript" src="app.js"></script>
    </head>
    <body>
        <div style="text-align: center;">
            <h1>자소서</h1>
            <hr>
        </div>
        <div>
            <% for (let i=0 ; i < posts.length ; i++) { %>
            <p> <a href='/resume/<%= posts[i].seq %>'> 지원 회사</a> :
                <%= JSON.stringify(posts[i].company) %>
            </p>
            <br>
            <%}%>
        </div>
        <div style="text-align: center;">
            <p>
                <a href="/resume/add"><input type="button" value="add resume"></a>
                <a href='../main'> <input type="button" value="main"> </a>
            </p>
        </div>
    </body>
</html>