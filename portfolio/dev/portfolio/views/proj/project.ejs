<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>project</title> 
        <script type="text/javascript" src="app.js"></script>
    </head>
    <body>
        <div style="text-align: center;">
            <h1> your project</h1>
            <hr>
        </div>
        <div>
            <% for (let i=0 ; i < posts.length ; i++) { %>
            <p> <a href='/project/<%= posts[i].seq %>'> 프로젝트명</a> :
                <%= JSON.stringify(posts[i].proj_name) %>
            </p>
            <p> url : <%= JSON.stringify(posts[i].url) %></p>
            <br>
            <%}%>
        </div>
        <div style="text-align: center;">
            <p>
                <a href="/project/add"><input type="button" value="add project"></a>
                <a href='../main'> <input type="button" value="main"> </a>
            </p>
        </div>
    </body>
</html>